import { FC } from 'react';

import styles from './styles.module.scss';

type ApplyingDocsProps = {
  className?: string;
};

export const ApplyingDocs:FC<ApplyingDocsProps> = ({ className }) => {
  return (
    <>
      Docs
    </>
  )
}
